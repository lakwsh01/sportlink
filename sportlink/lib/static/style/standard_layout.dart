import 'package:flutter/material.dart';

const standardAppbarPadding = EdgeInsets.all(12);
const standardContentPadding = EdgeInsets.all(12);
const standardPageMargin = EdgeInsets.all(12);
const standardInlineSectionPadding = EdgeInsets.all(12);
const standardInlinePadding = EdgeInsets.only(bottom: 12);
const standardBottomSheetMargin = EdgeInsets.all(0);
const standardBottomSheetPadding =
    EdgeInsets.only(left: 12, right: 12, top: 12);
